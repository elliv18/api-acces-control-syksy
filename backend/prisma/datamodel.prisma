enum UserType {
  ADMIN
  USER
}

type User {
    id: ID! @id
    userType: UserType!
    email: String! @unique
    password: String!
    apiKey: String
    createdAt: DateTime! @createdAt
    updatedAt: DateTime! @updatedAt
}
